bool function CheckAddress(string address)
{
	def number octets = 0;
	def number numbers = 0;	
	def ubyte addressSize = strlength(address);
	def string buff;
	def ubyte i;
	for i in 0..addressSize step ++i
	{
		if(address[i] <= '9' and address[i] >= '0')
		{
			if(numbers>3) => return false;
			buff[numbers] = address[i];
			++numbers;
		}
		elif(address[i] == '.')
		{
			if(ToNumber(buff) > 255 or numbers == 0) => return false;
			numbers = 0;
			++octets;
			buff = "";
		}
		else => return false;
	} 
	if(ToNumber(buff) > 255 or octets != 3) => return false;
	return true;
}

main
{
	def string ip;
	get ip;
	if(CheckAddress(ip) == false)
	{
		print "¬веден не правильный ip-адрес.";
		return -1;
	}
	print "¬ведены верные данные сети!";
	return 0;
}