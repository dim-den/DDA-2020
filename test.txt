number function fi(number x, number y) 
{
	def number z;
	if(x + 11 >= y) 
	{
		z=x*(x+y);
	}
	elif(x < y) 
	{
		z = y / x;
	} 
	elif (x == 15) 
	{
	z = x & y;
	} 
	else 
	{
		z=~y+x;
	}
	return z;  
}

main
{
	def number x = 13;
	
	def number z = fi(x, 39); 
	
	return 0; 
}
