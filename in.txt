number function fi(number x, number y) 
{
	def number z;
	if(x + 11 >= y) 
	{
		z=x*(x+y);
	}
	elif(x < y) 
	{
		z = x | y;
	} 
	elif (x == 15) 
	{
	z = x & y;
	} 
	else 
	{
		z=~y+x;
	}
	return z;  
}

bool function IsRoot(ubyte i, ubyte j)
{
	if(j == i * i) 
	{
 		return true;
 	}
 	return false;
}

main
{
	def number x = 13;
	def string sa = 'hey there';  
	def string sb;  
	if(strcopy(sb,sa) == true)
	{ 
		print 'успешное копирование'; 
	}
	else 
	{ 
		print 'скопировать строку не удалось'; 
	}  
	def bool res = strconcat(sa, ' you are welcome');
	def ubyte ua = 6;
	def ubyte ub = ua;
	def bool b = IsRoot(ua, ub);  
	def number z = fi(x, 11); 
	print sa;  
	print z;
	return 0; 
}
